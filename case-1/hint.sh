#!/usr/bin/env bash
HERE="$(cd "$(dirname "$0")" && pwd)"; P="$HERE/.hint"; n=$(cat "$P" 2>/dev/null||echo 0); n=$((n+1)); echo $n>"$P"
case $n in
 1) echo "🕴️ Capo: ถามตัวเองก่อน — งานนี้ 'หลุดออกนอกบ้าน' หรือยัง? (push ไปหรือยัง)";;
 2) echo "🕴️ Capo: ในเมื่อยังไม่หลุด เธอลบทิ้งให้เกลี้ยงได้เลย ไม่ต้องเก็บประวัติไว้";;
 3) echo "🕴️ Capo: ลองนึกถึงคำสั่งที่ 'ย้อน HEAD' กลับไป commit ก่อนหน้า แล้วลบของในมือด้วย";;
 *) echo "🕴️ Capo: ใบ้หมดแล้วรุกกี้ ไปลองเอง เดี๋ยวก็เป็น";;
esac
